{
  "name": "pi-redraw",
  "version": "0.1.0",
  "description": "Force a full pi TUI redraw without restarting pi.",
  "type": "module",
  "license": "MIT",
  "keywords": [
    "pi-package",
    "pi-extension",
    "tui",
    "redraw"
  ],
  "files": [
    "src",
    "README.md",
    "LICENSE"
  ],
  "pi": {
    "extensions": [
      "./src/index.ts"
    ]
  },
  "scripts": {
    "test": "node --experimental-strip-types --test test/*.test.ts",
    "typecheck": "tsc --noEmit"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": ">=0.84.3"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "0.84.3",
    "typescript": "^5.9.3"
  }
}
